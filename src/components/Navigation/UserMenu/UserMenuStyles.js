import styled from 'styled-components';

const StyledContainer = styled.div`
  display: flex;
  align-items: center;
`;

const StyledAvatar = styled.img`
  margin-right: 4px;
`;

const StyledName = styled.span`
  font-weight: 700;
  margin-right: 12px;
`;

export { StyledContainer, StyledAvatar, StyledName };
